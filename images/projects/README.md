# Project logos & covers

Your files in this folder (as uploaded):

| Client | File |
|--------|------|
| **Northwold** | `northwold-cover.png`, `northwold logo.jpg` |
| **CAMINEX** | `caminex cover.png`, `caminex logo.webp` |
| **KDCCI** | `kdcci cover.png`, `kdcci logo.webp` |

The site references these paths in `index.html`. Spaces in filenames are encoded as `%20` in URLs.

Optional: rename to simple names (e.g. `northwold-logo.jpg`) and update `index.html` if you prefer cleaner paths.
